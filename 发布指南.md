# xls-mcp-servers 发布指南

本文档说明如何将 `xls-mcp-servers` 发布到 PyPI 和 GitHub。

## 📋 发布前准备

### 1. 检查项目配置

确保以下文件已正确配置：

- ✅ `pyproject.toml` - 项目元数据和依赖
- ✅ `README.md` - 项目说明文档
- ✅ `LICENSE` - 许可证文件（如果使用MIT许可证）
- ✅ `.gitignore` - Git忽略文件配置

### 2. 更新版本号

在 `pyproject.toml` 中更新版本号：

```toml
[project]
version = "1.0.0"  # 更新为新版本，如 "1.0.1"
```

版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)：
- **主版本号**：不兼容的 API 修改
- **次版本号**：向下兼容的功能性新增
- **修订号**：向下兼容的问题修正

### 3. 准备 PyPI 账户

1. **注册 PyPI 账户**
   - 访问 https://pypi.org/account/register/
   - 注册账户并验证邮箱

2. **创建 API Token**（推荐）
   - 登录 PyPI
   - 进入 Account settings → API tokens
   - 创建新的 API token
   - 保存 token（只显示一次）

## 🚀 发布到 PyPI

### 方法1：使用 GitHub Actions 自动发布（推荐）

#### 步骤1：创建发布工作流

创建 `.github/workflows/publish.yml`：

```yaml
name: Publish to PyPI

on:
  release:
    types: [created]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish'
        required: true

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install build dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build twine
    
    - name: Build package
      run: python -m build
    
    - name: Publish to PyPI
      env:
        TWINE_USERNAME: __token__
        TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
      run: python -m twine upload dist/*
```

#### 步骤2：配置 GitHub Secrets

1. 在 GitHub 仓库中，进入 Settings → Secrets and variables → Actions
2. 点击 "New repository secret"
3. 添加以下 secret：
   - **Name**: `PYPI_API_TOKEN`
   - **Value**: 你的 PyPI API token

#### 步骤3：发布

**方式A：通过 GitHub Release 发布**
1. 在 GitHub 仓库中，点击 "Releases" → "Create a new release"
2. 填写版本号（如 `v1.0.0`）
3. 填写发布说明
4. 点击 "Publish release"
5. GitHub Actions 会自动构建并发布到 PyPI

**方式B：手动触发**
1. 在 GitHub 仓库中，进入 Actions
2. 选择 "Publish to PyPI" 工作流
3. 点击 "Run workflow"
4. 输入版本号并运行

### 方法2：手动发布

#### 步骤1：安装构建工具

```bash
pip install build twine
```

#### 步骤2：构建包

```bash
cd xls-mcp-servers
python -m build
```

这会生成 `dist/` 目录，包含：
- `xls_mcp_servers-1.0.0-py3-none-any.whl` (wheel包)
- `xls-mcp-servers-1.0.0.tar.gz` (源码包)

#### 步骤3：检查构建结果（可选）

```bash
twine check dist/*
```

#### 步骤4：上传到 PyPI

**使用 API Token（推荐）：**

```bash
twine upload dist/* --username __token__ --password <你的API_TOKEN>
```

**使用用户名密码：**

```bash
twine upload dist/*
# 然后输入用户名和密码
```

#### 步骤5：验证发布

访问 https://pypi.org/project/xls-mcp-servers/ 查看你的包。

## 📦 发布到 GitHub

### 1. 创建 GitHub 仓库

1. 访问 https://github.com/new
2. 创建新仓库，名称建议为 `xls-mcp-servers`
3. 不要初始化 README、.gitignore 或 LICENSE（如果本地已有）

### 2. 推送代码到 GitHub

```bash
cd xls-mcp-servers

# 初始化 Git（如果还没有）
git init

# 添加远程仓库
git remote add origin https://github.com/你的用户名/xls-mcp-servers.git

# 添加文件
git add .

# 提交
git commit -m "Initial commit: xls-mcp-servers v1.0.0"

# 推送到 GitHub
git branch -M main
git push -u origin main
```

### 3. 创建 Release

1. 在 GitHub 仓库中，点击 "Releases" → "Create a new release"
2. 选择或创建标签（如 `v1.0.0`）
3. 填写发布标题和说明
4. 可以附加文件（如源码包）
5. 点击 "Publish release"

## 🔧 在 Cursor 中使用

### 安装

发布到 PyPI 后，可以通过以下方式安装：

```bash
pip install xls-mcp-servers
```

### 配置 Cursor

在 Cursor 的 MCP 配置文件中添加：

**Windows:**
`%APPDATA%\Cursor\User\globalStorage\saoudrizwan.claude-dev\settings\cline_mcp_settings.json`

**Mac:**
`~/Library/Application Support/Cursor/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`

**Linux:**
`~/.config/Cursor/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`

添加配置：

```json
{
  "mcpServers": {
    "xls-mcp-servers": {
      "command": "xls-mcp-servers"
    }
  }
}
```

### 验证安装

1. 重启 Cursor
2. 在设置中查看 MCP 服务器列表
3. 应该能看到 `xls-mcp-servers` 并显示已启用的工具数量
4. 测试：让 AI 创建一个 Excel 文件，看是否能成功

## 📝 发布检查清单

发布前请确认：

- [ ] 版本号已更新
- [ ] `README.md` 已更新
- [ ] `pyproject.toml` 中的信息正确
- [ ] 所有测试通过
- [ ] 代码已提交到 Git
- [ ] GitHub Secrets 已配置（如果使用自动发布）
- [ ] LICENSE 文件存在
- [ ] `.gitignore` 已配置（排除 `dist/`, `build/` 等）

## 🔄 更新版本流程

1. **更新版本号**
   ```toml
   # pyproject.toml
   version = "1.0.1"  # 新版本号
   ```

2. **更新 CHANGELOG**（如果有）
   - 记录新功能和修复

3. **提交更改**
   ```bash
   git add .
   git commit -m "Bump version to 1.0.1"
   git push
   ```

4. **创建 Git 标签**
   ```bash
   git tag v1.0.1
   git push origin v1.0.1
   ```

5. **发布到 PyPI**
   - 如果使用 GitHub Actions，创建 Release 即可
   - 如果手动发布，执行构建和上传命令

6. **创建 GitHub Release**
   - 在 GitHub 上创建对应的 Release

## ⚠️ 常见问题

### 1. 上传失败：包已存在

**原因：** PyPI 不允许覆盖已发布的版本

**解决：** 更新版本号后重新发布

### 2. 认证失败

**原因：** API Token 错误或过期

**解决：** 
- 检查 token 是否正确
- 确认 token 有上传权限
- 重新生成 token

### 3. 构建失败

**原因：** 依赖问题或配置错误

**解决：**
- 检查 `pyproject.toml` 配置
- 确保所有依赖可用
- 运行 `python -m build` 查看详细错误

### 4. Cursor 中找不到工具

**原因：** MCP 服务器未正确启动

**解决：**
- 检查命令路径是否正确
- 确认包已正确安装：`pip show xls-mcp-servers`
- 检查命令是否可用：`xls-mcp-servers --help`（如果有）
- 查看 Cursor 的 MCP 日志

## 📚 相关资源

- [PyPI 文档](https://packaging.python.org/en/latest/)
- [Python Packaging User Guide](https://packaging.python.org/)
- [GitHub Actions 文档](https://docs.github.com/en/actions)
- [MCP 协议文档](https://modelcontextprotocol.io/)

## 📝 更新记录

- 2026-01-21: 创建发布指南
